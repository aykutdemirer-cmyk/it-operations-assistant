annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
asyncpg==0.31.0
# asyncssh — Faz 35 (Web SSH Terminal). EPL-2.0 lisans. Yalnızca backend'in
# kendisi bir SSH İSTEMCİSİ olarak hedef makineye bağlanmak için kullanır —
# kimlik bilgisi (username/password) HİÇBİR ZAMAN diske/DB'ye yazılmaz,
# yalnızca bellekte, bağlantı süresince tutulur (bkz. app/agents/ssh_proxy.py).
asyncssh==2.24.0
# bcrypt — Faz 46 (PAM/RBAC). Apache-2.0 lisans. Parola hash'leme
# (`app/auth/security.py`) — düz metin parola HİÇBİR ZAMAN DB'ye
# yazılmaz, yalnızca bcrypt hash'i (bkz. `users.password_hash`).
bcrypt==5.0.0
certifi==2026.7.22
cffi==2.1.1
click==8.4.2
colorama==0.4.6
cryptography==50.0.1
fastapi==0.141.1
h11==0.16.0
httpcore==1.0.9
httpcore2==2.12.0
httptools==0.8.0
httpx==0.28.1
httpx2==2.12.0
idna==3.19
iniconfig==2.3.0
# ldap3 — Faz 49 (LDAP/Active Directory Entegrasyonu). LGPL-3.0 lisans.
# `python-ldap` YERİNE tercih edildi — o OpenLDAP C başlıklarına karşı
# DERLENMESİ gerekiyor (bu Windows Server ortamında ek bir derleme
# zinciri gerektirirdi), `ldap3` saf Python (bkz. app/services/ldap.py).
ldap3==2.9.1
packaging==26.3
pluggy==1.6.0
pyasn1==0.6.4
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
Pygments==2.21.0
pysnmp==7.1.29
pytest==9.1.1
python-dotenv==1.2.3
# PyJWT — Faz 46 (PAM/RBAC). MIT lisans. `POST /api/auth/login`'in
# döndürdüğü kısa ömürlü oturum token'ının (`app/auth/security.py`)
# imzalanması/doğrulanması — anahtar `.env`'deki `JWT_SECRET_KEY`.
PyJWT==2.13.0
PyYAML==6.0.3
starlette==1.6.0
truststore==0.10.4
typing-inspection==0.4.4
typing_extensions==4.16.0
uvicorn==0.52.4
watchfiles==1.2.0
websockets==17.0.1

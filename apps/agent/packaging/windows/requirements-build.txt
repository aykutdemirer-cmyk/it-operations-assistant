# Yalnızca EXE build ETMEK için gerekli — agent'ın çalışma zamanı
# bağımlılığı DEĞİLDİR (bkz. apps/agent/requirements.txt: tek runtime
# bağımlılığı psutil). PyInstaller GPL-2.0-or-later + "bootloader
# exception" ile lisanslı — bootloader (derlenmiş stub) istisna
# kapsamında, PyInstaller'ın kendisi yalnızca BUILD aracı olarak
# kullanılır, agent'ın kendi kaynağına GPL şartı BULAŞTIRMAZ (bkz.
# docs/decisions.md).
pyinstaller==6.22.2

# Yalnızca Windows Servisi build'i (IT-Operations-Agent-Service.spec)
# için — SCM (Service Control Manager) protokolünü doğru implemente
# edebilmek üzere `agent/winservice.py` tarafından kullanılır (bkz. o
# modülün docstring'i — düz bir konsol EXE'si `sc.exe`'den gerçek bir
# graceful-stop sinyali ALAMAZ). PSF/BSD benzeri lisans, agent'ın
# runtime bağımlılığına (yalnızca psutil) hiç eklenmedi — yalnızca
# build-time.
pywin32==311
